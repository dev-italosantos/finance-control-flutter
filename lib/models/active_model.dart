class Active {
  String icon;
  String name;
  String symbol;
  double lastPrice;

  Active({
    required this.icon,
    required this.name,
    required this.symbol,
    required this.lastPrice
  });
}